<%= render 'form' %>
<link href="/assets/vendors/datepicker.css" rel="stylesheet" media="screen">
<link href="/assets/vendors/chosen.min.css" rel="stylesheet" media="screen">

<script src="/assets/vendors/jquery-1.9.1.js"></script>
<script src="/assets/bootstrap/js/bootstrap.min.js"></script>
<script src="/assets/vendors/jquery.uniform.min.js"></script>
<script src="/assets/vendors/chosen.jquery.min.js"></script>
<script src="/assets/vendors/bootstrap-datepicker.js"></script>

<script src="/assets/vendors/wysiwyg/wysihtml5-0.3.0.js"></script>
<script src="/assets/vendors/wysiwyg/bootstrap-wysihtml5.js"></script>

<script src="/assets/vendors/wizard/jquery.bootstrap.wizard.min.js"></script>

<!-- <script type="text/javascript" src="http://apis.daum.net/maps/maps3.js?apikey=405db4a3478f2fe559fd2b2d0d27856af183eb07" charset="utf-8"></script> -->
<script type="text/javascript">
	var map;
  var startLat = 37.5374;
  var startLng = 127.004;
	function init() {
    // 지도 출력
		map = new daum.maps.Map(document.getElementById('map'), {
			center: new daum.maps.LatLng(startLat, startLng),
			level: 3
		});
    
    // 마커 출력
		var marker = new daum.maps.Marker({
			position: new daum.maps.LatLng(startLat, startLng)
		});
		marker.setMap(map);
    
    daum.maps.event.addListener(map,"dragend",function(){
      // 좌표값 반환
			var center = map.getCenter();
			document.getElementById("mapLat").value = center.getLat();
      document.getElementById("mapLng").value = center.getLng();
      // 마커 이동
      var position = new daum.maps.LatLng(center.getLat(), center.getLng());
      marker.setPosition(position);
      marker.setMap(map);
    });
    
    daum.maps.event.addListener(map,"click",function(e) {
      // 마커 이동
      marker.setPosition(e.latLng);
      marker.setMap(map);
      // 좌표값 반환
			document.getElementById("mapLat").value = e.latLng.getLat();
      document.getElementById("mapLng").value = e.latLng.getLng();
    });
	}

  $(function() {
    init();
    //$(".datepicker").datepicker();
    //$(".chzn-select").chosen();
  });
</script>