 <div class="row-fluid">
  <!-- block -->
  <div class="block">
    <div class="navbar navbar-inner block-header">
        <div class="muted pull-left">배너 목록</div>
    </div>
    <div class="block-content collapse in">
      <div class="span12">
        <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered" id="example">
          <thead>
            <tr>
              <th>#</th>
              <th>제목</th>
              <th>등록일</th>
              <th>관리</th>
            </tr>
          </thead>
          <tbody>
            <% @banners.each do |banner| %>
              <tr class="">
                <td><%= banner.id %></td>
                <td><%= banner.title %></td>
                <td class="center"><%= banner.created_at %></td>
                <td class="center">
                  <%= link_to "상세정보", admin_banner_path(banner) %>
                  <%= link_to "수정", edit_admin_banner_path(banner) %>
                  <%= link_to "삭제", admin_banner_path(banner), method: :delete, confirm: "삭제 OK?" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
      <%= link_to "새 배너 등록", new_admin_banner_path, class: "btn btn-primary"%>
    </div>
  </div>
  <!-- /block -->
</div>
